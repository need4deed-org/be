export enum RoutePrefix {
  USER = "/user",
  PERSON = "/person",
  AUTH = "/auth",
  ME = "/me",
  LOGIN = "/login",
  REFRESH = "/refresh",
  HEALTH_CHECK = "/health-check",
  VERIFY_EMAIL = "/verify-email",
}
